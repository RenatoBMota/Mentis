{
  "name": "mentis",
  "version": "0.1.0",
  "private": true,
  "description": "Mentis — Sistema Clínico de Gestão para Psicólogos",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter ./apps/* dev",
    "dev:api": "pnpm --filter @mentis/api dev",
    "dev:web": "pnpm --filter @mentis/web dev",
    "build": "pnpm --recursive build",
    "lint": "pnpm --recursive lint",
    "test": "pnpm --recursive test",
    "db:generate": "pnpm --filter @mentis/api prisma generate",
    "db:migrate": "pnpm --filter @mentis/api prisma migrate dev",
    "prepare": "husky || true"
  }
}
